/**
 *1 create a new firebase projects in console.firebase.google.com
 *2 npm install firebase
 *3 create firebase.init.js and import getauth to export
 *4 firebase setting>authentication>enable email and password auth
 *5 create Login, signup component, setup route
 *6 attach form field handler and form submkit handler
 *7 npm install npm install --save react-firebase-hooks
 *8 useCreateUserWithEmailAndPassword from react firebase hooks
*9 if user is created redirect to the expected page
*10

/**
 * firebase hoisting
 * npm install -g firebase-tools
 * firebase login
 * firebase init
 * firebase deploy
 * 
 * 
 */